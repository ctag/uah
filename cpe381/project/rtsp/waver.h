#ifndef WAVER_H
#define WAVER_H

#include <iostream>
#include <fstream>

class waver
{
	public:
		/** Default constructor */
		waver();
		//waver(inFile, outFile);
		/** Default destructor */
		virtual ~waver();
	protected:
	private:
};

#endif // WAVER_H
